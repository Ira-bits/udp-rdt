{
  "name": "udp",
  "version": "1.0.0",
  "description": "CN A2",
  "main": "index.js",
  "scripts": {
    "compile": "npx tsc",
    "server": "npm run compile && node build/server.js",
    "client": "npm run compile && node build/client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.37",
    "typescript": "^4.2.4"
  }
}
